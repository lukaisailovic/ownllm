# Client API key required to call llmgate (compared timing-safe).
# REQUIRED when server.host is not loopback; optional for loopback-only binds.
LLMGATE_API_KEY=

# Log level: trace | debug | info | warn | error (default: info).
# Message bodies are only logged at debug.
LOG_LEVEL=info
